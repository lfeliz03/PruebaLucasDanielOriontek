@model IEnumerable<PruebaLucasFelizOriontek.Models.DireccionesClientes>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Clientes.Nombres)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Direcciones.Direccion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Clientes.Nombres)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Direcciones.Direccion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDireccionCliente }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDireccionCliente }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDireccionCliente })
        </td>
    </tr>
}

</table>
